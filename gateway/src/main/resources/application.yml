server:
  port: 8080



spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: storing-service
          uri: lb://STORING-SERVICE
          predicates:
            - Path=/api/storing/**

        - id: analysis-service
          uri: lb://ANALYSIS-SERVICE
          predicates:
            - Path=/api/analysis/**

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    url: /v3/api-docs
    # Ключевая настройка - указываем URL документации сервисов
    urls:
      - url: http://localhost:8081/v3/api-docs  # Прямой URL User Service
        name: Analysis Service
      - url: http://localhost:8082/v3/api-docs  # Прямой URL Order Service
        name: Storing Service

  # Отключаем ненужное
  show-actuator: false
  model-and-view-allowed: false

  # Настройки для корректной работы через Gateway
  use-fqn: false
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}